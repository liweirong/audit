<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iris.blog.mapper.BlogMapper">

	<select id="findList" resultType="com.iris.blog.entity.Blog">
		select * from blog t order by t.id desc
	</select>
</mapper>